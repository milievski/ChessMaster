#include "KnightMove.h"

KnightMove::KnightMove()
{

   
}
int KnightMove::validMove(vector< vector < Piece* > > &b, int ix, int iy, int dx, int dy)
{
    FriendlyPiece r;
    //check all the possible move that the knight can move if the move made is one of this the rule is passed
      if((ix+2 == dx && iy+1 == dy) || (ix+1 == dx && iy+2 == dy) || (ix-2 == dx && iy+1 == dy) || (ix-1 == dx && iy+2 == dy) || (ix-2 == dx && iy+1 == dy) || (ix-1 == dx && iy+2 == dy) || (ix+2 == dx && iy-1 == dy) || (ix+1 == dx && iy-2 == dy) || (ix-2 == dx && iy-1 == dy) || (ix-1 == dx && iy-2 == dy))
	 return r.validMove(b,ix,iy,dx,dy);

	//returns 0 if the move is not valid     
   return 0;

}
